{
  "name": "abtest-web-coding-challenge-2023",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --public-url /abtest/",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@babel/core": "7.2.0",
    "gh-pages": "^5.0.0",
    "parcel-bundler": "^1.6.1"
  },
  "keywords": [],
  "dependencies": {
    "ab-test": "^0.0.1"
  }
}
